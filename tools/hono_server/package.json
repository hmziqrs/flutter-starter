{
  "name": "starter_hono_server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The in-repo dummy backend: a static Hono (TypeScript) server implementing the app's /v1/* API contract for Flutter HTTP client dev and testing.",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "@hono/node-server": "^1.13.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.1.0",
    "tsx": "^4.19.0"
  }
}
